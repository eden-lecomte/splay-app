# Keep Windows batch files as CRLF (verbatim) so cmd.exe parses multi-line
# blocks correctly — including in the "Download ZIP" archive from GitHub.
*.bat -text
*.cmd -text

# Shell scripts must stay LF, even if checked out on Windows.
*.sh text eol=lf
